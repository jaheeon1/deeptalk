import { Layout,TeacherSwipe, MiddleNav, TalkSwipe, TalkModal} from "@/components";

function Landing() {
	return (
		<Layout>
			<TeacherSwipe />
            <TalkModal />
		</Layout>
	);
}

export default Landing;